# Packaging

This location contains subdirectories for each supported language (packaging format) with convenience scripts that wrap the manifest generation and add "packaging building" logic to provide a single end-to-end command that builds an installble package.

See each subdirectory for usage instructions:

- [Java](maven)

